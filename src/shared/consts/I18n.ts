import { TLanguages } from '../types';

export const FALLBACK_LNG: TLanguages = 'es';
export const LANGUAGES: TLanguages[] = [FALLBACK_LNG, 'en'];
export const DEFAULT_FILE_NAME = 'common';
export const I18N_COOKIE_NAME = 'i18next';
